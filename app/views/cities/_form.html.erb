<%= form_with(model: city) do |form| %>
  <% if city.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(city.errors.count, "error") %> prohibited this from being saved.</h2>
      <ul>
        <% city.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div>
    <%= form.label :name,"Name:", style: "display: block" %>
    <%= form.text_field :name %>
  </div>
  <div>
    <%= form.label :State_id, "State:", style: "display: block" %>
    <%= form.collection_select(:state_id, states, :id, :name, prompt: true) %>
  </div>
  <div>
    <%= form.submit "Save"%>
  </div>
<% end %>
